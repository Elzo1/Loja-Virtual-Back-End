spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/loja-virtual?useTimezone=true&serverTimezone=UTC
      username: root
      password: 882927
 
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false

server:
  error:
    include-message: never
      